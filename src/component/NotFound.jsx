import { Container } from "react-bootstrap"

export const NotFound = () => {
    return(
        <Container fluid className="body-area fs-1">
           Error --- 없는 Page입니다 
        </Container>
    )
}